# Signal CRM — required environment variables
#
# Copy this file to .env.local (local dev) or add these to
# Vercel → Settings → Environment Variables (production).
#
# Get your Convex URL by running: npx convex url

# The Convex deployment URL for the client (REQUIRED)
# Format: https://<your-deployment>.convex.cloud
VITE_CONVEX_URL= https://brainy-echidna-665.convex.cloud
